{
  "name": "spansim",
  "version": "0.1.0",
  "private": true,
  "description": "Spånsim – simulator för hobby-CNC (GRBL) med materialborttagning och skärfysik",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "vite build --mode single",
    "preview": "vite preview",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "three": "^0.186.1"
  },
  "devDependencies": {
    "vite": "^8.3.1",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
